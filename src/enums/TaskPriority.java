package enums;

/**
 * Enumeração que representa as prioridades das tarefas no sistema operativo.
 * Pode assumir os valores de LOW_PRIORITY (Baixa Prioridade) ou HIGH_PRIORITY
 * (Alta Prioridade).
 *
 * @see TaskPriority
 */
public enum TaskPriority {
    LOW_PRIORITY,
    HIGH_PRIORITY
}
